import ProductForm from "@/components/containers/ProductForm";

const page = () => {
  return (
    <section className="p-6">
      <ProductForm type="create" />
    </section>
  );
};

export default page;
